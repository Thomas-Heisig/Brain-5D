# 2026-09-05 — Adaptive Wesen frontend redesign

## Implemented

- Added a dedicated `Wesen` workspace as the live machine-native body view.
- Removed `Network` from primary frontend navigation.
- Moved the legacy Release/Gate workspace to a footer button.
- Simplified `Embodiment` to the technical sensor/device/actuator/body-boundary surface.
- Applied the shared dark technical visual language across dashboard surfaces.
- Replaced the fixed seven-node organism layout with dynamic morphology derived from `/api/embodiment/connections`.
- Added separate sensor and actuator branches, generic unknown connections and explicit unavailable placeholders.
- Recomputed the visible body membrane from current observed endpoints so device appearance/loss changes morphology.
- Added data pins on body nodes, interactive node inspection, wheel zoom and whole-body reset.
- Expanded interoception with CPU, RAM, temperature, fan, disk and runtime pacing fields when the backend exposes them.
- Added adaptive visual profiles for resource pressure, thermal pressure, sensor degradation, actuator failure and network isolation where reported.
- Added a delayed self-model that mirrors the same current morphology and uses observed recurrence/loopback latency when available.
- Added session-local morphology history and event filtering.
- Added a recurrence mini-chart for the last 100 observed samples.
- Added visual causal-path emphasis while retaining the distinction between connectivity and experimentally established causality.
- Kept the complete `Wesen` surface read-only: no `/api/control`, actuator writes, learning execution or language output.
- Added regression coverage for dynamic morphology, read-only boundaries, sensor/actuator representation, self-model, zoom, history, navigation and responsive/reduced-motion behavior.

## Scientific interpretation

The page is an operator visualization, not evidence of consciousness, self-awareness or emotion. Recurrence, loopback latency and adaptive body-state rendering are technical observables/derived presentation state. Scientific causal claims continue to require controlled protocols, receipts, DATA and accepted EVID artifacts.
