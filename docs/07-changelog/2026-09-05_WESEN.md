# 2026-09-05 — Adaptive Wesen frontend redesign

## Implemented

- Added a dedicated `Wesen` workspace as the live machine-native body view.
- Removed `Network` from primary frontend navigation.
- Moved the legacy Release/Gate workspace to a footer button.
- Simplified `Embodiment` to the technical sensor/device/actuator/body-boundary surface.
- Applied the shared dark technical visual language across dashboard surfaces.
- Replaced the fixed seven-node organism layout with dynamic morphology derived from `/api/embodiment/connections`.
- Added separate sensor and actuator branches, generic unknown connections and explicit unavailable placeholders.
- Added a bounded force-directed presentation layer so body nodes no longer depend on fixed coordinates.
- Replaced the static envelope with a padded convex-hull membrane around the currently observed body nodes.
- Added external environment satellites for reported network/weather/camera/audio sources.
- Added pointer-centered wheel zoom, pointer-drag pan, focus fading and double-click reset.
- Expanded interoception with CPU, RAM, temperature, fan, disk and runtime pacing fields when the backend exposes them.
- Added differentiated presentation states for resource pressure, thermal pressure, sensor degradation, actuator failure, network isolation, recovery and unknown telemetry.
- Added data pins on body nodes and interactive node inspection.
- Added a delayed self-model backed by a bounded browser frame ring buffer; when loopback latency is measured, the mirror selects an actually earlier observed body frame.
- Added browser-local morphology snapshots and a timeline scrubber for operator time-travel inspection.
- Added a recurrence mini-chart for the last 100 observed samples.
- Added visual causal-path emphasis and optional causal tracer labels sourced only from observed event/decision/action/receipt identifiers.
- Added centralized neutral terminology constants for SNN core, sensor/actuator endpoints, interoception, environment and feedback.
- Kept the complete `Wesen` surface read-only: no `/api/control`, actuator writes, learning execution or language output.
- Added regression coverage for dynamic morphology, read-only boundaries, force layout, hull membrane, satellites, delayed mirror, camera, time-travel history, terminology, navigation and reduced-motion behavior.
- Removed the superseded unreferenced first organism prototype after review; only the stable v2 layer is wired into the dashboard.

## Scientific interpretation

The page is an operator visualization, not evidence of consciousness, self-awareness or emotion. Recurrence, loopback latency and adaptive body-state rendering are technical observables/derived presentation state. Browser-local morphology snapshots are not scientific persistence. Visual causal tracers do not establish causality without a controlled protocol, receipts, DATA and accepted EVID artifacts.
