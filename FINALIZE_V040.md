# Finalize v0.4.0

This patch aligns Runtime Checkpoint V3 with the already V3-aware core restore,
keeps the dashboard JSON contract strict, and changes the package version to 0.4.0.

Run the full quality gate before tagging.
