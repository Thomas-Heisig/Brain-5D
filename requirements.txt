# ============================================================================
# Brain-5D — requirements.txt
# ============================================================================
# NOTE: The authoritative dependency declaration lives in pyproject.toml
# under [project] and [project.optional-dependencies].
#
# This file is generated/maintained for tools that do not read
# pyproject.toml (e.g. pip-compile, some CI systems).
#
# Install via:  pip install -e ".[dev]"
# ============================================================================

# Core dependencies (from pyproject.toml [project])
PyYAML>=6.0
matplotlib>=3.8
numpy>=1.26
psutil>=5.9
psutil>=5.9

# Development dependencies (from pyproject.toml [project.optional-dependencies] dev)
pytest>=8.0

# Optional: DOCX-Unterstützung  (docs extra in pyproject.toml)
python-docx>=1.2.0

# Optional: XLSX-Unterstützung  (docs extra in pyproject.toml)
openpyxl>=3.1.5

# Optional: PDF-Unterstützung  (docs extra in pyproject.toml)
pypdf>=6.0.0
