"use strict";
import { apiGet } from "../core/api.js";
let refreshTimer=null;
function esc(v){const d=document.createElement("div");d.textContent=v==null?"":String(v);return d.innerHTML;}
function open(path){if(!path)return;document.dispatchEvent(new CustomEvent("brain5d:open-file",{detail:{source:"research",path}}));window.MHRNWorkspaceArchitecture?.selectRoute?.("science","files");}
function ensure(){let p=document.getElementById("mhrn-research-docs");if(p)return p;const root=document.getElementById("tab-research");if(!root)return null;p=document.createElement("section");p.id="mhrn-research-docs";p.className="mhrn-research-docs card";p.dataset.panelInfo="Registry-Index für Research-Artefakte. Inhalte rendert ausschließlich der zentrale File Viewer.";p.innerHTML=`<header><div><span class="workspace-kicker">REGISTRY · REPORTS · AIRR</span><h2>Research-Dokumente & Berichte</h2><p>Index und Provenienz; keine parallele Dateiansicht.</p></div><span id="research-docs-badge">lade …</span></header><div class="research-docs-grid"><section class="research-docs-section"><h3>Research-Dokumente</h3><div id="research-documents-list">lade …</div></section><section class="research-docs-section"><h3>Generierte Berichte</h3><div id="research-reports-list">lade …</div></section><section class="research-docs-section"><h3>AI Research Reports</h3><div id="research-ai-reports-list">lade …</div></section></div>`;const target=root.querySelector('.research-subpanel[data-subpanel="registry"]')||root;target.querySelector("#research-registry-placeholder")?.remove();target.append(p);p.onclick=e=>{const b=e.target.closest("[data-research-path]");if(b)open(b.dataset.researchPath);};return p;}
function row(title,meta,path,extra=""){return `<article class="research-item"><div><strong>${esc(title||path||"—")}</strong><small>${esc(meta||"")}</small>${extra}</div><button type="button" data-research-path="${esc(path||"")}" ${path?"":"disabled"}>Im File Viewer öffnen</button></article>`;}
function docs(payload){const a=Array.isArray(payload?.documents)?payload.documents:[];document.getElementById("research-documents-list").innerHTML=a.length?a.map(x=>row(x.name||x.title||x.path,[x.category,x.kind].filter(Boolean).join(" · "),x.path)).join(""):"<p>Keine Research-Dokumente.</p>";return a.length;}
function reports(payload){const a=Array.isArray(payload?.reports)?payload.reports:[];document.getElementById("research-reports-list").innerHTML=a.length?a.map(x=>row(x.name||x.title||x.path,x.size_bytes!=null?`${x.size_bytes} B`:"generated report",x.path)).join(""):"<p>Keine generierten Berichte.</p>";return a.length;}
function airr(payload){const a=Array.isArray(payload?.reports)?payload.reports:[];document.getElementById("research-ai-reports-list").innerHTML=a.length?a.map(x=>{const path=x.json_path||x.markdown_path;const extra=x.markdown_path&&x.markdown_path!==path?`<button type="button" data-research-path="${esc(x.markdown_path)}">Markdown öffnen</button>`:"";return row(x.report_id||path,[x.experiment_id,x.report_id,x.size_bytes!=null?`${x.size_bytes} B`:""].filter(Boolean).join(" · "),path,extra);}).join(""):"<p>Keine AI Research Reports.</p>";return a.length;}
async function refresh(){if(!ensure())return;const [d,r,a]=await Promise.allSettled([apiGet("/api/research/documents"),apiGet("/api/research/reports"),apiGet("/api/research/ai-reports")]);let total=0;if(d.status==="fulfilled")total+=docs(d.value);else document.getElementById("research-documents-list").textContent=`Nicht verfügbar: ${d.reason.message}`;if(r.status==="fulfilled")total+=reports(r.value);else document.getElementById("research-reports-list").textContent=`Nicht verfügbar: ${r.reason.message}`;if(a.status==="fulfilled")total+=airr(a.value);else document.getElementById("research-ai-reports-list").textContent=`Nicht verfügbar: ${a.reason.message}`;document.getElementById("research-docs-badge").textContent=`${total} artifact(s)`;}
export function initResearchDocs(){ensure();refresh();if(refreshTimer)clearInterval(refreshTimer);refreshTimer=setInterval(refresh,30000);}
